/** Count/alert red — in-app badges and chrome.action toolbar badge. */
export const destructiveColor = '#e5484d';

/** Text on destructive badges — matches in-app `text-white` on `bg-destructive`. */
export const destructiveForegroundColor = '#ffffff';

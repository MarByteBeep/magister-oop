# Finalize

Never use `cd ... &&`.
Run commands directly.

Run:

bun run check
bun run check:ci
bun run check:fallow

Fix all issues found.

Repeat until all commands pass.

Only finish when everything is green.